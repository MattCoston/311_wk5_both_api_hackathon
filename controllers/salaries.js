const mysql = require('mysql');
const pool = require("../mysql/connection");
const { apiErrorHandler } = require('../error/ApiError');

const getTopPaid = (req,res) => {

}

const getByRange = (req, res) => {
    
}